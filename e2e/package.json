{
  "name": "jimbo-connect-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end tests for Intro Connect, run against a live deployment.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
