# Intro Connect session handoff, written 2026-08-12 late afternoon

Paste-ready prompt for a fresh Claude session. Everything below reflects state
on THIS machine as of writing; nothing here has been pushed to GitHub.

---

Take over the Intro Connect project mid-stream. Prior session context:

**Anchor.** "Intro Connect" is the product; the repo/folder is
`C:\Users\sweis\jimbo-connect` (Vite/React frontend built with **yarn, never
npm ci**; FastAPI backend, dev API at localhost:8001, prod on Render). Root the
session there. Frontend tests: `npm test` in `frontend/` (node --test).
Backend tests: `.\.venv\Scripts\python.exe -m pytest -q` from `backend/`.

**Git state (all local, nothing pushed).** Branch `main`, ahead of
`origin/main` by one held commit `b174b57` (Audience Republic import
recognition; Scott chose to hold it, do not push without asking). On top of
that sits UNCOMMITTED, fully green work fixing silent failure in the invite
email path: send-before-record + per-reason failure reporting in
`backend/invites.py` and `backend/attendee_import.py`, shared
`backend/send_report.py`, honest toasts via `frontend/src/lib/inviteResult.js`
and `importResult.js` wired into `MyEvents.jsx` and `StartFromRoster.jsx`,
plus tests (backend 612 pass, frontend 77 pass, build clean).
`frontend/package-lock.json` is deleted (staged) - it was stale and broke
npm ci; yarn.lock is canonical. First decision for Scott: commit this work
(separate commit from b174b57), and whether to push both.

**Cold outreach campaign (the day's main thread).** The "zero replies from
140 emails" mystery is solved and documented in
`growth/campaign/targets-and-tracker.md` (see Send log section): the 140 sends
were an unvetted chamber-directory scrape, unpersonalized "Greetings," copy,
~half to generic role inboxes, several to the wrong person entirely, from a
Gmail account whose display name is still **"rob Alexander"** (a phishing-red-flag
mismatch with the Scott Weiss signature - STILL UNFIXED, keep nudging Scott:
Google Account > Personal info > Name for introconnectme@gmail.com).
Deliverability was verified fine (mail-tester 10/10, domain on no blocklists).

Today a corrected wave of **12 personalized emails went out** (2:17-2:46 PM,
from introconnectme@gmail.com, Gmail account slot /u/3 in Scott's Chrome) to
named Colorado event organizers. Targets + scores:
`growth/campaign/targets-named-organizers.csv`. Exact copy sent:
`growth/campaign/drafts-2026-08-12.md`. Vetted version of the old scrape:
`growth/campaign/targets-colorado-vetted.csv`.

Campaign rules (from `growth/campaign/gmail-intro-template.md`): any reply
stops that thread's sequence same day and gets logged in the tracker; business
hours weekdays only; follow-ups go in the same thread; never lead with the
founding discount; pricing answers exact ($39/$390 Starter, $99/$990 Pro,
guests never pay); plain voice, no em dashes, no emoji in outreach copy.

Scheduled work:
- **Fri 2026-08-15: touch 2** (day-3 follow-up, in-thread) for all 12 threads
  with no reply. Template 2 in `gmail-intro-template.md`, personalized the
  same way. Prior session created drafts via Chrome automation (Compose
  overlay, Tab between fields, Escape saves the draft) - do the same, drafts
  only, Scott sends.
- **Wed 2026-08-20: touch 3** (day-8 last touch), same mechanics.
- **138 old "Greetings" drafts** still sit in that Gmail's Drafts folder =
  the retired bad batch. Marked do-not-send in the tracker. Scott has not yet
  approved discarding them - ask once, then act.

**Render.** `jimbo-connect-api` (srv-d7scltv7f7vs73fj2p30) alerted "Exited
with status 3" today, auto-recovered; verified healthy afterward (/docs 200).
Root cause unread - Render dashboard needs Scott to click the GitHub SSO
login. Optional: verify `.github/workflows/keep-warm.yml` actually pings the
live API URL (https://jimbo-connect-api-rdkp.onrender.com).

**Working style (Scott).** One instruction at a time; multiple-choice
questions only when a decision is genuinely his; articulate what you're doing
as you do it; every file path handed to him must be complete and absolute;
measure before diagnosing - his repeated experience is that the confident
first hypothesis is wrong. "Start session" / "close session" trigger the
git sync protocol in `C:\Users\sweis\.claude\CLAUDE.md`. Session-boundary
sync matters extra here because none of today's work is pushed: if the next
session is on ANOTHER machine, none of this exists there until this machine
commits and pushes.
